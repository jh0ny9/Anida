window.ANIDA_CONFIG = {
  // Cambia estos valores en producción sin tocar la estructura de la web.
  // provider: 'local' guarda provisionalmente en localStorage.
  // provider: 'endpoint' envía JSON al endpoint indicado para conectar Brevo, Mailchimp, MailerLite o una base de datos propia.
  formProvider: 'local',
  resourceEndpoint: '',
  contactEndpoint: '',
  resourceDownloadUrl: 'assets/recurso-anida-provisional.pdf'
};
